# Sample Outputs (Optional)

This folder is reserved for small, curated example outputs (e.g., exported CSVs or screenshots)
used for documentation or quick validation.

It is intentionally kept minimal to avoid committing large simulation artifacts.
